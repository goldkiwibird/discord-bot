# 로컬에서 테스트 213건이 통과한 조합으로 고정 (버전을 풀어두면 재배포 때마다 최신을 받아,
# 코드를 한 줄도 안 고쳤는데 죽는 상황이 생긴다). Python 버전은 저장소 루트의 .python-version.
discord.py==2.7.1
aiohttp==3.14.3
asyncpg==0.31.0
tzdata==2024.1
python-dotenv==1.0.0
